name: Apply
on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          role-to-assume: "arn:aws:iam::392017163980:role/core-infra-role-staging"
          role-session-name: "oidc-access-staging"
          aws-region: "eu-west-1"

      - name: Check AWS Access
        run: aws sts get-caller-identity

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v4
        with:
          context: app
          file: app/Dockerfile
          push: true
          tags: ${{ steps.login-ecr.outputs.registry }}/sbp-assignment-staging:${{ github.sha }}

      - name: Deploy to App Runner
        id: deploy-apprunner
        uses: awslabs/amazon-app-runner-deploy@main
        with:
          service: app-runner-image-deploy-service
          image: ${{ steps.build-image.outputs.image }}
          access-role-arn: arn:aws:iam::392017163980:role/app-runner-service-role
          runtime: NODEJS_12
          region: ${{ secrets.AWS_REGION }}
          cpu: 1
          memory: 2
          port: 3000

      - name: App Runner output
        run: echo "App runner output ${{ steps.deploy-apprunner.outputs.service-id }}"
